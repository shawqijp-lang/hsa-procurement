# ==========================================
# إعدادات Azure PostgreSQL للإنتاج
# ==========================================
# انسخ هذا الملف إلى .env على خادم الإنتاج
# وعدّل القيم حسب إعدادات Azure الخاصة بك

# ==========================================
# قاعدة البيانات الرئيسية (Azure PostgreSQL)
# ==========================================

# معلومات اتصال Azure PostgreSQL
AZURE_HOST=hsa-production-db.postgres.database.azure.com
AZURE_PORT=5432
AZURE_DB=hsa_production
AZURE_USER=hsaadmin
AZURE_PASSWORD=كلمة-السر-القوية-هنا

# Connection String الكامل للإنتاج (مع SSL و search_path)
DATABASE_URL_PROD=postgresql://hsaadmin:كلمة-السر-القوية-هنا@hsa-production-db.postgres.database.azure.com:5432/hsa_production?sslmode=require&options=-c%20search_path=production

# ==========================================
# إعدادات التطبيق
# ==========================================

NODE_ENV=production
PORT=5000

# مفتاح JWT (أنشئ مفتاح عشوائي قوي)
JWT_SECRET=مفتاح-عشوائي-قوي-جداً-غيّره-الآن

# ==========================================
# إعدادات اختيارية
# ==========================================

# إذا كنت تستخدم Claude AI للتحليل الذكي
# ANTHROPIC_API_KEY=your-anthropic-key-here

# إذا كنت تستخدم OpenAI للتحليل
# OPENAI_API_KEY=your-openai-key-here

# ==========================================
# للتطوير فقط (على Replit)
# ==========================================
# لا تضع DATABASE_URL_PROD هنا في بيئة التطوير!
# DATABASE_URL=postgresql://...@neon.tech/dev_database?options=-c search_path=development

# ==========================================
# ملاحظات أمنية مهمة
# ==========================================
# 1. لا تشارك هذا الملف في Git (.gitignore)
# 2. استخدم كلمات سر قوية (16+ حرف)
# 3. فعّل Firewall في Azure (IPs محددة فقط)
# 4. استخدم SSL دائماً (sslmode=require)
# 5. غيّر JWT_SECRET إلى قيمة عشوائية فريدة
